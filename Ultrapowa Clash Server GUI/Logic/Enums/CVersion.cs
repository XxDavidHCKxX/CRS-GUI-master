namespace CRS.Logic.Enums
{
    /// <summary>
    /// <see cref="CVersion"/> is an enum containing the version of the client which is accepted by the server for now.
    /// </summary>
    public enum CVersion
    {
        Major = 2,
        Minor = 0,
        Build = 2000
    }
}
